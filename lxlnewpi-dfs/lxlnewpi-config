# build release (bookworm) and arch (armhf)
# update RELEASE for new release!
# Don't forget to update stage0/prerun.sh
RELEASE=bookworm
ARCH=armhf

# pi-gen issue
PI_GEN="pi-gen-lxlnewpi"
PI_GEN_REPO="https://github.com/ElTopo/pi-gen-lxlnewpi"

# the name of the image to build
IMG_NAME=lxlnewpios-$RELEASE-$ARCH
# deploy format
DEPLOY_COMPRESSION=none
#DEPLOY_COMPRESSION=zip
# default system locale
LOCALE_DEFAULT='en_US.UTF-8'
# hostname, discoverable as 'lxlnewpi.local'
TARGET_HOSTNAME=lxlnewpi
# default keyboard keymap
KEYBOARD_KEYMAP=us
# dfault keyboard layout
KEYBOARD_LAYOUT='English (US)'
# default system timezone
TIMEZONE_DEFAULT='America/Los_Angeles'
# first user name (uid: 1000)
FIRST_USER_NAME=lxl
# first user's password
FIRST_USER_PASS=1234567890
# disable the renaming of the first user during the first boot
DISABLE_FIRST_BOOT_USER_RENAME=1
# default WLAN regulatory domain and unblocks WLAN interfaces
WPA_COUNTRY=US
# enable ssh server
ENABLE_SSH=1
